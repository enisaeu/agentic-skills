# References

Load only the reference needed for the current package manager. Do not load every file.

Exact manager references: npm, pnpm, yarn, pip, poetry, pipenv, uv, maven, gradle, cargo, go, nuget, conda, mamba, rubygems, bundler, composer, swiftpm, cocoapods, cpan, cran, renv.

Family routers: pypi, maven-gradle, ruby, swift. Use them only when the exact manager is not obvious.

Common references: package-confusion, examples, other-ecosystems.

Each manager file follows `package-manager-reference-template.md`: scope, files, command safety, selection checks, integration controls, monitor and mitigate.
